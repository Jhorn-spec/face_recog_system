# face_recog_system
one-shot face recognition

# activate the environment and install the required dependencies
# run 'streamlit run main.py'

Using FastApi Export endpoints like:
POST /register_face
POST /verify_face
GET /user/{id}